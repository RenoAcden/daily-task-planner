import React from 'react';
import ReactDOM from 'react-dom/client';
import DailyTaskPlanner from './DailyTaskPlanner.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DailyTaskPlanner />
  </React.StrictMode>,
);
