apiVersion: v1
kind: Service
metadata:
  name: daily-task-planner-svc
  labels:
    app: daily-task-planner
spec:
  type: NodePort
  selector:
    app: daily-task-planner
  ports:
  - port: 5050
    targetPort: 80
    nodePort: 30005
    protocol: TCP
